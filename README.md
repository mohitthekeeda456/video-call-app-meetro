# Meetro

Free-to-run meetings app starter with:

- React frontend
- Node.js + Express backend
- Socket.io signaling
- WebRTC media
- MongoDB persistence

## Run

1. Install dependencies in `frontend` and `backend`.
2. Set `MONGODB_URI` and `JWT_SECRET`.
3. Start the backend and frontend dev servers.
